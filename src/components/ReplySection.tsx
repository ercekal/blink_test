const ReplySection = () => {
  return (
    <div>
      <form>
        <input type="text" />
        <button type="submit">Enter</button>
      </form>
    </div>
  );
};

export default ReplySection;
